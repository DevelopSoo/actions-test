coverage:
  status:
    project: #add everything under here, more options at https://docs.codecov.com/docs/commit-status
      default:
        # basic
        target: auto # 이전 커밋의 커버리지를 기준으로 자동으로 목표치 설정
        threshold: 0% # 커버리지가 조금이라도 떨어지면 실패 처리 (0% 변동만 허용)
        base: auto # 비교 기준을 자동으로 설정 (보통 base 브랜치의 최신 커밋)
    patch: # 이 부분 추가
      default:
        target: 80% # 원하는 목표치로 조정 가능

comment: #this is a top-level key
  layout: 'reach, diff, flags, files' # PR 코멘트에 표시할 내용 구성
  behavior: default
  require_changes: false # if true: only post the comment if coverage changes
  require_base: false # [true :: must have a base report to post]
  require_head: true # [true :: must have a head report to post]
  hide_project_coverage: false # [true :: only show coverage on the git diff aka patch coverage]
  show_carryforward_flags: true # 이 부분 추가
